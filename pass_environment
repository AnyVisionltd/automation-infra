username=${USER}
userhome=${HOME}
host_hostname=$(hostname)
host_ip=$(hostname -I | awk '{print $1}')
uid=${UID}
workdir=${PWD}
DOCKER_GROUP_ID=$(getent group docker | cut -d: -f3)
color_prompt=yes
HABERTEST_HEARTBEAT_SERVER=${HABERTEST_HEARTBEAT_SERVER}
HABERTEST_PROVISIONER=${HABERTEST_PROVISIONER}
WORKSPACE_TOP=${WORKSPACE_TOP}
TESTS_JENKINS_RUN=${TESTS_JENKINS_RUN}
